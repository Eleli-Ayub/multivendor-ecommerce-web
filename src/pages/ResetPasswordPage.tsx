import PasswordResetForm from '../components/Auth/ResetPassword';

const ResetPasswordPage = () => {
    return (
        <div className="pt-20 bg-gray-light h-screen ">
            <PasswordResetForm />
        </div>
    );
};

export default ResetPasswordPage;
