info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/users/signin
  body:
    type: json
    data: |-
      {
        "username":"kamah@gmail.com",
        "password":"passwordsecure"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
